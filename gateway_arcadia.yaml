apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: arcadia-main-gateway
spec:
  selector:
    istio: istio-system/ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: arcadia-np-main
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: mainsrv
spec:
  hosts:
  - "*"
  gateways:
  - arcadia-main-gateway
  http:
  - match:
    - uri:
       prefix: /
    route:
    - destination:
         host: arcadia-np-main 
         port:
           number: 80 

